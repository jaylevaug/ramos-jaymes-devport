- name: Build project
        run: |
          # The '|| true' helps bypass some non-critical errors
          bun run build
        env:
          VITE_LOVABLE_TAGGER: false
          CI: false # This prevents some frameworks from crashing on warnings
